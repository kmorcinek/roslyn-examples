# it should be an console application in C#/.NET

dotnet Version 6
using Roslyn.
to apply changes to whole project (path to codebase passed as parameter).

## The change would be

update code to improve performance. From

```
Where.(...).Count();
```

into:

```
Count(...);
```

create some simple 2 UT to test it. in folder /tests place cs files that should be updated.
